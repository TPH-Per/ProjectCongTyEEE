# 📝 BÁO CÁO CÔNG VIỆC & BỐI CẢNH HÔM NAY (18/07/2026 — 15:20)

## 🎯 TỔNG QUAN

Buổi làm việc tiếp tục tập trung vào việc hoàn thiện hệ thống màn hình reception — từ sửa lỗi build, fix lỗi logic đặt bàn, tối ưu UI/UX báo cáo, bổ sung dữ liệu mock cho reports, cho đến enhance form đặt bàn thủ công với validation nâng cao và quick tags.

---

## 🛠️ CHI TIẾT CÁC CÔNG VIỆC ĐÃ HOÀN THÀNH

### ✅ 1. FIX LỖI BUILD & SYNC DB TABLES VÀO RECEPTION ORDER VIEW

**File:** `src/views/reception/ReceptionOrderView.vue`

- Phát hiện lỗi runtime: popup "Không tìm thấy bàn A01 trên hệ thống" khi auto-assign bàn.
- **Nguyên nhân:** `restaurantStore.areas` chứa dữ liệu mock hardcoded (A01, B01, C01...) không bao giờ sync từ DB thật. Khi `assignQuickTable()` tìm được bàn A01 từ mock data rồi lookup trong `dbTablesList` (từ `hall_list_tables` RPC) → không khớp → throw error.
- **Fix:** Trong `fetchDbTables()`, sau khi load `dbTablesList` từ DB, đồng bộ `restaurantStore.areas` từ DB data — group theo zone, map status (`available` → `Available`, `occupied` → `Serving`, `reserved` → `Reserved`), giữ mock data chỉ khi DB trống.
- Khi DB có dữ liệu, mock data được thay thế bằng bàn thật → UI và logic lookup dùng cùng nguồn → không còn mismatch.

---

### ✅ 2. CHỈNH SỬA CONTEXT MENU KHI DOUBLE-CLICK BÀN

**File:** `src/views/reception/ReceptionOrderView.vue`

- **Yêu cầu:** Khi double-click vào bàn đã có khách → hiển thị đủ 6 action (Chọn món, Chuyển bàn, Ghép phiếu, Tách phiếu, Tách món, Hủy phiếu). Bàn trống → chỉ hiển thị "Chọn món".
- **Thay đổi:**
  - `tableActions` chuyển từ `ref` (hardcoded array) → `computed`: kiểm tra `selectedTableForAction.status === 'Available'` → lọc chỉ giữ `select_items`; bàn có khách → trả đủ 6 actions.
  - Template: bỏ `:disabled` + `opacity-40 pointer-events-none` vì các action không hợp lệ giờ bị lọc khỏi danh sách hiển thị — UI gọn hơn, không còn nút mờ vô hiệu.

---

### ✅ 3. KHỞI TẠO DỮ LIỆU TĨNH CHO FLOOR PLAN

**Files:** `src/views/admin/AdminFloorsView.vue`, `src/stores/restaurantStore.ts`

- **Vấn đề:** `AdminFloorsView.vue` (route `/reception/floors`) khởi tạo `areas = ref<AreaInfo[]>([])` — rỗng cho đến khi `loadTables()` fetch từ DB. Nếu DB trống → floor plan trắng hoàn toàn.
- **Fix:** Khởi tạo `areas` từ dữ liệu tĩnh của `restaurantStore.areas` (deep clone) làm fallback. Dữ liệu mock khớp đúng `Databan.txt`: 10 khu (A, B, C, R, T, Capichi, Shopee, BE, Grab, Catalog) với tổng cộng ~50 bàn.
- Khi DB có dữ liệu thật, `loadTables()` ghi đè; DB trống thì floor plan vẫn hiển thị đủ bàn.

---

### ✅ 4. FIX UI/UX MÀN HÌNH REPORTS (`/reception/reports`)

**File:** `src/views/reception/ReportsView.vue`

Nhận diện và sửa nhiều vấn đề layout + visual:

#### 4.1. Layout — Bỏ Triple Scroll
- **Trước:** Page container dùng `min-height: 100vh; overflow-y: auto` → toàn trang scroll + 2 bảng con cũng scroll → 3 thanh cuộn chồng nhau, trải nghiệm rất kém.
- **Sau:** Đổi sang `height: 100vh; overflow: hidden` — chỉ 2 bảng (data table + detail table) scroll nội bộ, page không scroll. Thêm `min-height: 0` cho `.main-content` (bắt buộc để flex children shrink đúng).

#### 4.2. Flexible Heights
- `.main-table-wrapper`: bỏ `height: 340px` cứng → `flex: 1; min-height: 0` (tự co giãn theo viewport).
- `.detail-section`: bỏ `height: 420px` cứng → `flex: 0 0 38%; min-height: 180px; max-height: 45%` (tỷ lệ linh hoạt).
- Trước đó trên màn hình nhỏ, bảng bị cắt; trên màn lớn, bảng quá ngắn → giờ tự fit.

#### 4.3. Color Scheme Thống Nhất Navy
- **Trước:** Đa màu loạn — header trắng, table header xanh lá (#10b981), active tab xanh lá, sub-tab active xanh dương, scrollbar xanh lá, row-selected outline xanh dương → không nhất quán.
- **Sau:** Thống nhất palette navy (#1e3a5f / #1a5276):
  - Header: gradient navy `#1e3a5f → #1a5276`
  - Table headers: navy `#1e3a5f` (thay vì #10b981)
  - Active tab: navy
  - Active sub-tab: navy `#1a5276`
  - Scrollbars: navy
  - Row-selected outline: navy
  - Summary footer: giữ dark slate `#1e293b` — hài hòa với navy
- Header buttons: đổi từ multi-color (xanh/tím/cam/đỏ) → trắng trong suốt `rgba(255,255,255,0.15)` trên nền navy, border `rgba(255,255,255,0.2)`.

#### 4.4. UX Improvements
- Hover states: thêm cho tab buttons (`bg-gray-100 → #e2e8f0`)
- Sub-tab hover: color transition `rgba(255,255,255,0.6) → 0.9`
- Table `th`: thêm `white-space: nowrap` chống wrap header text
- Bỏ các `.sticky` CSS rules không sử dụng (dead code)

---

### ✅ 5. FIX LỖI "MÓN ĂN KHÔNG HỢP LỆ" TRONG CUSTOMER CART

**Files:** `src/stores/customerStore.ts`, `src/views/customer/CustomerCart.vue`, `src/services/customerApi.ts`

**Lỗi:** Popup "Món ăn 'Vé Người Lớn 1380' có mã không hợp lệ" xuất hiện khi khách gửi đơn.

**Nguyên nhân gốc:**
- `menuData.ts` dùng mock IDs (`bf1390-1`, `bf1150-2`...) không phải UUID.
- `loadMenu()` thay thế bằng UUID thật từ DB theo tên món, nhưng nếu tên không khớp DB → mock ID giữ nguyên.
- `isValidUUID()` fail khi `submitOrder()` → popup lỗi generic, không có option phục hồi.
- Thêm nữa, mock mode (Supabase chưa cấu hình) vẫn bị chặn bởi UUID check dù không cần DB.

**4 fix áp dụng (adapt theo kiến trúc Supabase + Pinia, KHÔNG dùng fetch/Express trong prompt):**

1. **`loadMenu()` (customerStore.ts):** Sau khi substitute UUID, items không có DB match → set `is_available = false` → không thể thêm vào giỏ hàng.

2. **`submitOrder()` (CustomerCart.vue):** UUID validation chỉ chạy ở live mode (bỏ qua mock mode). Popup cũ (1 nút OK, generic message) → dialog 3 nút:
   - **"Xóa món lỗi"** — gọi `removeInvalidCartItems()`, tiếp tục đặt món còn lại. Nếu giỏ trống sau khi xóa → hiển thị thông báo riêng.
   - **"Tải lại thực đơn"** — gọi `loadMenu()` để re-sync UUID từ DB.
   - **"Hủy"** — đóng dialog, không làm gì.

3. **`removeInvalidCartItems()` (customerStore.ts):** Method mới — filter cart items có UUID hợp lệ, return tên các món đã xóa để hiển thị toast notification.

4. **`addToCart()` (customerStore.ts):** Deduplication cải thiện — trước đó merge chỉ theo `menuItemId`, giờ merge theo `menuItemId` + `note` (cho phép thêm cùng món với note khác nhau thành 2 line items riêng biệt).

---

### ✅ 6. TẠO 20 MOCK RECORDS CHO REPORTS VIEW

**File:** `src/views/reception/ReportsView.vue`

- Thay thế 11 records cũ (data `02/07/2026`) bằng 20 records mới, data `18/07/2026`:
  - **Restaurant (12):** Khu A (A01-A05), Khu B (B01-B02), Khu C (C01, C03), Khu R (R01-R02), Khu T (T01) — đủ status: paid, no_invoice, shortage, ordering, cancelled, gift, hold, recall
  - **Order Online (4):** Grab01, Shopee01, BE01, CP01
  - **Retail (2):** Quầy bán lẻ
  - **Distribution (2):** Đại lý A, B
- **Giá thực tế buffet Nhật Ngưu Cát:**
  - Vé Người Lớn 1380 = 1.388.000đ/vé, VAT 8%
  - Đơn nhà hàng: 2 khách = ~3M, 4 khách = ~6M, 10 khách = ~15M
  - Distribution: ~8M-16M/thùng
- **`selectRow()` dynamic:** Thay hardcoded items theo ID bằng generator động — click bất kỳ row nào → items table tự sinh detail dựa trên `type`, `slKhach`, `tienHang`, `thue`, `giam` của row đó.
- Filter mặc định cập nhật `fromDate/toDate = 2026-07-18`, `itemsPerPage` tăng 12 → 20 để hiển thị hết.
- `sampleItems` default cập nhật: `Vé Người Lớn 1380` thay vì `Buffet Lẩu 399k` cũ.

---

### ✅ 7. ENHANCE FORM ĐẶT BÀN THỦ CÔNG (CREATE BOOKING MODAL)

**File:** `src/views/admin/AdminFloorsView.vue`

**Quyết định kiến trúc quan trọng:** Prompt đề xuất install `@headlessui/vue` và tạo component riêng `CreateReservationModal.vue`. Tuy nhiên, project **không dùng** `@headlessui/vue` — tất cả modals đều custom Tailwind inline. Thay vì thêm dependency mới và tạo component riêng, tôi **enhance trực tiếp modal "Tạo Đơn Đặt Bàn" sẵn có** trong `AdminFloorsView.vue`, giữ đúng conventions (light theme, `bg-white`, `rounded-3xl`, `border-pink-100`, `#FF7B89` accent).

**6 enhancements:**

1. **Inline validation** — `bookingErrors` reactive object, validate:
   - Tên khách: required (không rỗng)
   - SĐT: regex `^(0|\+84)[3-9]\d{8}$` (hỗ trợ 0xx và +84xx)
   - Giờ đến: required
   - Số khách: 1-50
   - Bỏ Swal.fire generic, thay bằng error text đỏ dưới mỗi field + border đỏ `border-red-400`.

2. **Quick time buttons** — `+15p` và `+1g` bên cạnh time input. Hàm `setQuickBookingTime(minutes)` set giờ = now + offset, format `HH:MM`.

3. **Guest counter** — Thay plain `<input type="number">` bằng nút `−` / `+` hai bên (giống prompt). Hàm `incrementGuests()` / `decrementGuests()`, giới hạn 1-50.

4. **Quick tags** — 8 tag (Sinh nhật, Khách VIP, Gia đình, Hẹn đối tác, Gần cửa sổ, Yên tĩnh, Dị ứng hải sản, Ăn chay). Click toggle → `toggleQuickTag(tag)` tự động prepend vào notes dạng `[Sinh nhật, Khách VIP] ...`, **giữ nguyên** nội dung user đã nhập phía sau tags (không overwrite).

5. **Loading state** — Submit button hiện spinner SVG + "Đang lưu..." khi `isSubmittingBooking = true`, disabled trong 600ms simulate async. `saveNewBooking()` chia thành `saveNewBooking()` (entry point + validation + loading) và `_doSaveBooking()` (logic tạo/sửa booking).

6. **Modal widen** — `max-w-md` → `max-w-lg`, thêm `max-h-[90vh] overflow-y-auto` cho form dài hơn. Header có gradient icon `from-pink-500 to-red-500` giống prompt design. Dọn dẹp: bỏ import `TimePicker15` (không còn dùng), thêm `reactive` vào import list.

---

## 📈 KẾT QUẢ ĐẠT ĐƯỢC

| # | Task | Status |
|---|------|--------|
| 1 | Fix lỗi "Không tìm thấy bàn A01" | ✅ |
| 2 | Context menu: 6 actions cho bàn có khách, 1 cho bàn trống | ✅ |
| 3 | Dữ liệu tĩnh floor plan từ `Databan.txt` | ✅ |
| 4 | Fix UI/UX reports: bỏ triple scroll, flexible heights, navy palette | ✅ |
| 5 | Fix lỗi "Món ăn không hợp lệ" trong customer cart | ✅ |
| 6 | 20 mock records cho reports với giá buffet thực tế | ✅ |
| 7 | Enhance form đặt bàn: validation, tags, counter, loading | ✅ |

---

## ✅ KIỂM TRA XÁC NHẬN BUILD

Tất cả thay đổi đã được verify bằng `npm run build` (`vue-tsc -b && vite build`):

- `vue-tsc` type-check: **PASS** (0 errors)
- Vite build: **PASS** — ~3899 modules transformed, ~6.5s
- Chỉ có warning về chunk size > 500 kB (chunk `index-*.js` ~3.2 MB) — đây là gợi ý tối ưu code-splitting, không phải lỗi.

---

## 📌 KẾT LUẬN

Buổi làm việc hôm nay tập trung vào việc ổn định UI/UX và sửa lỗi logic trên nhiều màn hình reception khác nhau. Các fix đều được adapt theo kiến trúc thực tế của project (Vue 3 + Supabase + Pinia, custom Tailwind modals — không phải `@headlessui/vue`), đảm bảo code clean, nhất quán và dễ bảo trì.

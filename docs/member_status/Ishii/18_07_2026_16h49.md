# 📝 BÁO CÁO CÔNG VIỆC & BỐI CẢNH (18/07/2026 — 16:49)

## 🎯 TỔNG QUAN

Hoàn thiện hệ thống mock data toàn diện: tất cả các API calls trong `customerApi.ts` giờ đều có mock fallback khi Supabase không cấu hình. App chạy 100% client-side, không cần backend. Thêm banner thông báo Mock Mode toàn cục.

---

## 🛠️ CHI TIẾT CÁC CÔNG VIỆC ĐÃ HOÀN THÀNH

### ✅ 1. MOCK FALLBACK CHO TOÀN BỘ CUSTOMER API

**File:** `src/services/customerApi.ts`

Bổ sung mock fallback cho các method còn thiếu (session trước đã có `getAreas`, `getTables`, `getRawMenuItems`). Giờ **tất cả** method đều có guard `isSupabaseConfigured`:

| Method | Mock behavior |
|---|---|
| `authenticateStaff` | Hardcode `123456`/`654321` (đã có sẵn) |
| `getAreas` | Trả 5 khu mock (A, B, C, R, T) |
| `getTables` | Trả bàn mock theo zone (A01-A09, B01-B03, v.v.) |
| `getRawMenuItems` | Generate UUID ổn định từ `menuTemplate` |
| `selectTable` | **Mới** — return `{ success: true }` cho mock tableId |
| `confirmTable` | **Mới** — return session `sess-mock-<timestamp>` |
| `releaseTable` | **Mới** — no-op |
| `createOrder` | **Mới** — return order `ord-mock-<timestamp>`, delay 800ms |
| `getOrderHistory` | **Mới** — return `[]` |
| `submitServiceRequest` | **Mới** — return request `sr-mock-<timestamp>` |
| `getServiceRequests` | **Mới** — return `[]` |
| `updateServiceRequest` | **Mới** — no-op |
| `submitFeedback` | **Mới** — return feedback `fb-mock-<timestamp>` |
| `subscribeToTableUpdates` | **Mới** — return no-op unsubscribe |

**Quyết định kiến trúc:** KHÔNG tạo parallel system (`services/mockApi.js`, `composables/useCart.js`) như prompt gốc đề xuất. Lý do:
- Project đã có kiến trúc Pinia stores + `customerApi.ts` interface
- Tạo layer song song sẽ phải sửa lại tất cả components đang dùng `customerStore`
- Mock fallback trong `customerApi.ts` transparent — callers không cần biết mock hay real
- Khi cấu hình `.env.local`, `isSupabaseConfigured` flip → tự động switch sang DB thật, 0 code change

---

### ✅ 2. MOCK MODE NOTICE BANNER

**File mới:** `src/components/MockModeNotice.vue`

- Fixed top banner, gradient amber→red, `animate-pulse`
- Text: `⚠️ MOCK MODE — Không gọi API/Database`
- Chỉ render khi `isSupabaseConfigured === false`
- `z-[9999]` — hiển thị trên mọi element

**Tích hợp vào `App.vue`:**

```vue
<template>
  <div id="app" :lang="currentLang">
    <MockModeNotice />
    <RouterView />
  </div>
</template>
```

→ Banner hiện trên **mọi page** khi chạy offline, biến mất khi cấu hình Supabase thật.

---

## 📊 TỔNG HỢP MOCK DATA SYSTEM HOÀN CHỈNH

| Layer | File | Offline behavior |
|---|---|---|
| **Supabase client** | `lib/supabase.ts` | `isSupabaseConfigured = false`, client placeholder |
| **Customer API** | `services/customerApi.ts` | 14 method đều có mock fallback |
| **Customer store** | `stores/customerStore.ts` | `isMockMode` flag, `loadMenu()` substitute UUID |
| **Restaurant store** | `stores/restaurantStore.ts` | 10 khu, ~50 bàn, 12 bookings, 8 table orders (dates updated 18/07) |
| **Reception order** | `views/reception/ReceptionOrderView.vue` | `fetchDbTables()` sync từ store, `mockUnassignedReservations` (5 entries) |
| **Reports** | `views/reception/ReportsView.vue` | 20 mock invoices (Restaurant + Online + Retail + Distribution) |
| **Floor plan** | `views/admin/AdminFloorsView.vue` | `areas` khởi tạo từ `restaurantStore`, `selectedDate = today` |
| **Menu data** | `data/menuData.ts` | ~60 items (buffet, lunch, dinner, drinks, à la carte) |
| **Notice banner** | `components/MockModeNotice.vue` | Hiện trên mọi page khi offline |

---

## ✅ KIỂM TRA XÁC NHẬN BUILD

`npm run build` (`vue-tsc -b && vite build`): **PASS** — 0 errors, ~17.5s

---

## 📌 KẾT LUẬN

Hệ thống hiện chạy hoàn toàn offline mà không cần Supabase backend. Tất cả flows chính đều hoạt động:
- ✅ Customer: chọn khu → chọn bàn → duyệt menu → thêm giỏ hàng → đặt món
- ✅ Reception: floor plan, order view, reports, đặt bàn thủ công
- � Admin: floor plan management, table modal, booking modal (validation + tags + loading)

Khi cấu hình Supabase thật (`.env.local` có `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY`), mọi thứ tự động switch sang DB thật — banner biến mất, API calls đi đến Supabase, không cần thay đổi code.
